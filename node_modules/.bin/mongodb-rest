#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../mongodb-rest/bin/mongodb-rest" "$@"
  ret=$?
else 
  node  "$basedir/../mongodb-rest/bin/mongodb-rest" "$@"
  ret=$?
fi
exit $ret
